<div class="nested-fields well well-compact">
  <div class="form-group">
    <%= f.label :name, "カテゴリー名：" %>
    <%= f.text_field :name %><br>
    <%= link_to_remove_association "このカテゴリーを削除", f %>
  </div>

  <div class="middle_categories">
    <%= f.fields_for :middle_categories do |middle_category| %>
      <%= render 'middle_category_fields', f: middle_category %>
    <%end%>
    <div class="links">
      <%= link_to_add_association "品種を追加", f, :middle_categories %>
    </div>
  </div>
</div>